---
import ConsentManagerHead from '../../components/ConsentManagerHead.astro';
import '../../styles/global.css';
import LegalPage from '../../components/LegalPage.astro';
import { legalContent } from '../../data/legalContent';
import { absoluteUrl } from '../../lib/siteMetadata';

const content = legalContent.de.privacy;
const homeHref = '/de/';
const alternateLocaleHref = '/en/privacy/';
const faviconHref = `${import.meta.env.BASE_URL}favicon.png`;
const canonicalUrl = absoluteUrl('/de/datenschutz/');
const alternateUrl = absoluteUrl(alternateLocaleHref);
---

<html lang="de">
	<head>
		<ConsentManagerHead locale="de" />
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href={faviconHref} />
		<title>{content.metaTitle}</title>
		<meta name="description" content={content.metaDescription} />
		<link rel="canonical" href={canonicalUrl} />
		<link rel="alternate" hreflang="de" href={canonicalUrl} />
		<link rel="alternate" hreflang="en" href={alternateUrl} />
		<link rel="alternate" hreflang="x-default" href={canonicalUrl} />
	</head>

	<body class="bg-white text-slate-900">
		<LegalPage
			content={content}
			homeHref={homeHref}
			alternateLocaleHref={alternateLocaleHref}
			currentLocale="de"
		/>
	</body>
</html>
